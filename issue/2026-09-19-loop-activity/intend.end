사용자 의도
루프 엔지니어링에서 어제 실제로 동작한 Slack 공용 수신·번역·기본 응답이 어제 필터에 나타나야 한다.
7일에는 보이고 어제에는 안 보이는 원인을 해결한다. 다른 루프도 실제 활동일 기준으로 집계한다.
Claude 세션과 직접 API 호출을 구분하되 같은 루프에서 확인하고, 같은 호출을 중복 계산하지 않는다.
의도와 실행 지시문을 파일로 남기고, 지시문에 따라 구현·검증한다.
