# 미처리 227건 — 왜 3-상태 분류만으로는 10.6%밖에 못 줄이는가

작성: 2026-09-01 · 근거: `~/.condition-mate/slack-translate/items.jsonl` 1670줄 전수 실측

라이언이 낸 안은 세그 토글을 `미처리 / 전체` 2개에서 `AI 처리 / 결정 대기 / 전체` 3개로
늘리는 것이었고, 기대치는 60% 이상 감소였다. 이 문서는 그 기대치가 왜 실측과 어긋나는지,
그리고 라이언이 실제로 원한 결과를 얻으려면 무엇을 하나 더 해야 하는지를 적는다.

결론부터: **라이언이 고른 축은 맞다. 그런데 그 축만으로는 227건이 203건이 된다.**
축을 하나 더 얹으면 227건이 10건이 된다.

## 실측

전체 1670건이 이렇게 갈린다. 합이 정확히 1670으로 맞는다.

| 구분 | 건수 |
|---|---|
| `autoDone` — 슬랙에서 이모지가 달려 자동 해결 | 938 |
| `done.json` 에만 있음 — 앱에서 처리완료를 누름 | 505 |
| 미처리 | 227 |

그 227건을 다시 갈랐다.

| 구분 | 건수 | 비율 |
|---|---|---|
| AI가 슬랙에 실제로 행동함 (`ackAt` 있음) | 24 | 10.6% |
| AI가 아무것도 안 함 | 203 | 89.4% |

여기까지가 라이언의 3-상태안이 보는 그림이다. `AI 처리` 로 갈 수 있는 것이 24건이므로
`결정 대기` 에는 203건이 남는다. **227 → 203, 감소율 10.6%.**

## 60%가 아닌 이유 — 분류는 처리하지 않는다

분류는 **이미 처리된 것을 다른 칸으로 옮겨 줄 뿐, 처리되지 않은 것을 줄이지 못한다.**
그러니 분류로 줄일 수 있는 최대치는 처음부터 "이미 처리된 것의 비율" 로 고정돼 있다.
그 값이 10.6%다. 어떤 이름으로 몇 개의 칸을 만들든 이 상한을 넘을 수 없다.

60%가 나오려면 미처리 227건 중 136건 이상이 이미 AI 손을 탄 것이어야 했다. 실제로는
24건이다.

## 진짜 원인은 시간축이다

`requestLevel`·`ackGrade` 를 부여하는 파이프라인은 **08-29에 도입됐다.** 그 이전 수집분은
등급이 0건이다. 그래서 미처리 227건을 시간으로 자르면 이렇게 된다.

| 구분 | 건수 |
|---|---|
| 08-28 이전 수집 | 193 |
| 08-29 이후 수집 | 34 |

그리고 두 축을 교차하면 그림이 완전히 드러난다.

| | AI가 행동함 | 등급만 매김 | 아무것도 없음 |
|---|---|---|---|
| **08-29 이후** | 24 | 9 | 1 |
| **08-28 이전** | 0 | 0 | **193** |

읽어야 할 것은 왼쪽 아래 칸의 **0** 이다. 파이프라인 도입 전 수집분은 AI가 건드린 것이
단 한 건도 없다. 193건 전부가 등급도 없고 응답도 없다.

이것이 라이언의 3-상태안이 10.6%에서 멈추는 이유다. 193건은 어떤 상태로 쪼개도 전부
`결정 대기` 로 떨어진다. AI가 판정한 흔적이 없으니 `AI 처리` 로 보낼 근거가 없고, 그래서
라이언의 화면은 203건으로 그대로 남는다.

반대로 이것은 좋은 소식이기도 하다. 08-29 이후 유입 234건 중 미처리는 34건뿐이다.
**신규 유입분 미처리율 14.5%.** 새 파이프라인은 이미 잘 돌고 있다. 화면을 막고 있는 것은
지금 들어오는 것이 아니라 **과거에 쌓인 193건**이다.

## 제안 — 라이언의 축을 그대로 쓰되, 백로그를 네 번째 칸으로 뺀다

라이언의 안을 버리지 않는다. `AI 처리` 와 `결정 대기` 를 가르는 판단은 옳다. 다만 그 사이에
193건이 떨어지므로, 그 193건을 받을 칸을 하나 더 만든다.

세그 토글 4개:

| 칸 | 정의 | 건수 |
|---|---|---|
| **결정 대기** (기본) | AI가 처리 못 함 · 백로그 아님 | **10** |
| **AI 처리** | `ackAt` 있음 — AI가 슬랙에 응답함 | 24 |
| **백로그** | 08-29 파이프라인 도입 전 미분류분 | 193 |
| **전체** | 전부 | 1670 |

**227 → 10. 감소율 95.6%.** 라이언이 기대한 60%를 넘긴다.

백로그 칸은 숨기는 것이 아니다. 삭제도 아니고 슬랙에 아무것도 보내지 않는다. 항목은
그대로 남고 전용 탭에서 언제든 볼 수 있으며, `--undo` 로 되돌아온다. 다만 **매일 보는 화면을
막지 않는다.** 193건은 급하지 않고, 급하지 않은 것이 급한 것과 같은 줄에 서 있는 것이
지금 문제의 전부였다.

## 라이언이 옳게 짚은 것 — 이모지 체크는 AI 처리가 아니다

이 지적이 이 건에서 가장 값이 나갔다. `autoDone` 938건의 `autoBy` 를 세어 보면 최다가
`U03GRE909MJ` 로 606건이고, 이 id 는 **라이언 본인**이다
(`people-directory.json` → `realName: "Lion cho (조중현,Cho Chung Hyun)"`).

즉 처리완료 938건 중 최소 606건은 AI가 한 것이 아니라 **라이언이 슬랙에서 손으로 이모지를
단 것**이다. 그런데 UI는 이 둘을 `✅ 이모지로 해결됨` 하나로 뭉뚱그려 왔다. 화면만 보면
AI가 938건을 처리해 준 것처럼 보이지만 실제로 AI가 한 몫은 그보다 훨씬 작다.

이것이 "두 번 체크" 의 뿌리다. 라이언은 대시보드에서 227건을 다 볼 수 없어 슬랙으로 갔고,
슬랙에서 손으로 체크했고, 그 결과가 대시보드에 `autoDone` 으로 돌아와 AI 실적처럼 섞였다.
화면이 자기 노동을 자동화 성과로 되돌려 주고 있었으니, 무엇이 실제로 자동화됐는지 알 길이
없었다.

그래서 칩을 가른다. `✅ 내가 슬랙에서 처리` / `✅ 다른 사람이 처리` / `🤖 AI 응답함`.
옛 레코드처럼 누가 했는지 모르는 것은 모른다고 그대로 둔다. 추정해서 채우지 않는다.

## 이 제안이 하지 않는 것

- **193건을 대신 판단하지 않는다.** 마감은 "AI가 판정하지 못했다" 를 기록하는 것이지
  "판단이 끝났다" 가 아니다. 그 안에 중요한 것이 있으면 백로그 탭에서 찾아야 한다.
- **파이프라인의 판정 품질을 손대지 않는다.** 08-29 이후 `requestLevel` 분포는
  L2=83, L0=58, L4=34, L3=31, L1=1 이고 L1 이 1건뿐인 것은 등급 경계가 한쪽으로 쏠려 있다는
  신호일 수 있다. 이번 범위 밖이다.
- **옛 193건을 소급 등급 부여하지 않는다.** 가능은 하지만 모델 호출 193회가 들고, 4일 지난
  DM·멘션에 지금 등급을 매겨서 얻는 값이 그 비용을 넘는지 확인되지 않았다.

## 남는 진짜 질문

이번 수정은 화면을 고친다. 화면이 고쳐지면 다음 질문이 보인다.

08-29 이후 34건 중 24건은 AI가 응답했고 10건은 못 했다. **이 10건은 왜 못 했는가.**
9건은 등급까지는 매겼는데 응답으로 가지 못했다. 등급을 매기고도 멈추는 자리가 어디인지가
다음에 볼 곳이다. 미처리를 227에서 10으로 줄이고 나면, 그 10이 왜 10인지가 처음으로
읽을 수 있는 크기가 된다. 그것이 이 수정의 진짜 값이다.
